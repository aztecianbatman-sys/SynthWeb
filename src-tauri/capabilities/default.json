{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for the Synth Browser shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
